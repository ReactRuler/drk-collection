{% comment %}
  Customer Account Dashboard - Main Account Page
{% endcomment %}

<div class="account-dashboard" style="background-color: #050505; padding: 80px 20px; min-height: 80vh;">
  <div class="page-width">
    
    <div class="account-header" style="margin-bottom: 60px;">
      <h1 style="color: #D2D2D2; font-size: 48px; font-weight: 300; margin: 0 0 10px; text-transform: uppercase; letter-spacing: 4px;">
        My Account
      </h1>
      <p style="color: #767676; font-size: 16px; margin: 0;">
        Welcome, {{ customer.first_name }}!
      </p>
    </div>

    <div class="account-container" style="display: grid; grid-template-columns: 350px 1fr; gap: 40px; max-width: 1400px; margin: 0 auto;">
      
      {% comment %} Sidebar Navigation {% endcomment %}
      <aside class="account-sidebar" style="background: rgba(255,255,255,0.02); border-radius: 10px; padding: 40px; border: 1px solid rgba(125,125,125,0.5); height: fit-content;">
        <nav class="account-nav" style="display: flex; flex-direction: column; gap: 20px;">
          <a href="{{ routes.account_url }}" 
             class="nav-item active"
             style="color: #D2D2D2; text-decoration: none; font-size: 14px; text-transform: uppercase; letter-spacing: 1px; padding: 15px 20px; border-radius: 10px; background: rgba(124,124,124,0.27); transition: all 0.3s;">
            Account Information
          </a>
          <a href="{{ routes.account_addresses_url }}" 
             class="nav-item"
             style="color: #767676; text-decoration: none; font-size: 14px; text-transform: uppercase; letter-spacing: 1px; padding: 15px 20px; border-radius: 10px; transition: all 0.3s;">
            Addresses
          </a>
          <a href="{{ routes.account_orders_url }}" 
             class="nav-item"
             style="color: #767676; text-decoration: none; font-size: 14px; text-transform: uppercase; letter-spacing: 1px; padding: 15px 20px; border-radius: 10px; transition: all 0.3s;">
            My Orders
          </a>
          <a href="{{ routes.account_logout_url }}" 
             class="nav-item"
             style="color: #767676; text-decoration: none; font-size: 14px; text-transform: uppercase; letter-spacing: 1px; padding: 15px 20px; border-radius: 10px; transition: all 0.3s;">
            Logout
          </a>
        </nav>
      </aside>

      {% comment %} Main Content {% endcomment %}
      <div class="account-content" style="background: rgba(255,255,255,0.02); border-radius: 10px; padding: 40px; border: 1px solid rgba(125,125,125,0.5);">
        <h2 style="color: #D2D2D2; font-size: 32px; font-weight: 300; margin: 0 0 30px; text-transform: uppercase; letter-spacing: 2px;">
          Account Information
        </h2>

        <div class="account-info" style="margin-bottom: 40px;">
          <div style="margin-bottom: 25px;">
            <label style="display: block; color: #767676; font-size: 12px; text-transform: uppercase; letter-spacing: 1px; margin-bottom: 8px;">
              Name
            </label>
            <p style="color: #D2D2D2; font-size: 16px; margin: 0;">
              {{ customer.name }}
            </p>
          </div>

          <div style="margin-bottom: 25px;">
            <label style="display: block; color: #767676; font-size: 12px; text-transform: uppercase; letter-spacing: 1px; margin-bottom: 8px;">
              Email
            </label>
            <p style="color: #D2D2D2; font-size: 16px; margin: 0;">
              {{ customer.email }}
            </p>
          </div>

          {% if customer.phone %}
            <div style="margin-bottom: 25px;">
              <label style="display: block; color: #767676; font-size: 12px; text-transform: uppercase; letter-spacing: 1px; margin-bottom: 8px;">
                Phone
              </label>
              <p style="color: #D2D2D2; font-size: 16px; margin: 0;">
                {{ customer.phone }}
              </p>
            </div>
          {% endif %}
        </div>

        <a href="{{ routes.account_edit_url }}" 
           style="display: inline-block; padding: 15px 40px; border: 1px solid rgba(124,124,124,0.27); border-radius: 23px; background: rgba(124,124,124,0.27); color: #D2D2D2; text-decoration: none; text-transform: uppercase; letter-spacing: 2px; font-size: 14px; transition: all 0.3s;">
          Edit Account
        </a>
      </div>

    </div>
  </div>
</div>

<style>
  .account-nav .nav-item:hover {
    background: rgba(124,124,124,0.27) !important;
    color: #D2D2D2 !important;
  }
  
  .account-content a:hover {
    background: rgba(124,124,124,0.4) !important;
    border-color: rgba(124,124,124,0.4) !important;
  }
  
  @media (max-width: 1024px) {
    .account-container {
      grid-template-columns: 1fr !important;
    }
  }
</style>

